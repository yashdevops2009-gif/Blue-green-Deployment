apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: frontend
    version: green
  ports:
    - port: 80
      targetPort: 3200
      nodePort: 30080
